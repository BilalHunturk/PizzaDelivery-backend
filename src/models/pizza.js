class Pizza {
    constructor(id, name, size, dough_size) {
      this.id = id;
      this.name = name;
      this.size = size;
      this.dough_size = dough_size;
    }
  }
  
  module.exports = Pizza;